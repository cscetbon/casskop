name: Bootstrap and CI images

on:
  workflow_dispatch:

jobs:

  # dgoss-tests:
  #   runs-on: ubuntu-latest
  #   container: orangeopensource/casskop-build:v1.13.0
  #   needs: docker-images
  #   if: ${{ !startsWith(github.ref, 'refs/tags/') }}

  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v3

  #     - name: Run Dgoss Tests
  #       run: |
  #         curl -L https://raw.githubusercontent.com/aelsabbahy/goss/master/extras/dgoss/dgoss -o /usr/local/bin/dgoss
  #         curl -L https://github.com/aelsabbahy/goss/releases/download/v0.3.6/goss-linux-amd64 -o /usr/local/bin/goss
  #         chmod +rx /usr/local/bin/dgoss /usr/local/bin/goss
  #         export GOSS_PATH=/usr/local/bin/goss GOSS_FILES_STRATEGY=cp IMAGE_TO_TEST=orangeopensource/cassandra-bootstrap:0.1.9
  #         ./docker/bootstrap/dgoss/runChecks.sh

  bootstrap-image:
    runs-on: ubuntu-latest
    permissions:
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Log in to the Container registry
        uses: docker/login-action@v1.14.1
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v2.10.0
        with:
          context: docker/bootstrap
          tags: ghcr.io/cscetbon/casskop-bootstrap:${{ github.ref_name }}
          # push: true

  build-image:
    runs-on: ubuntu-latest
    permissions:
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Log in to the Container registry
        uses: docker/login-action@v1.14.1
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v2.10.0
        with:
          file: docker/ci/Dockerfile
          tags: |
            ghcr.io/cscetbon/casskop-build:latest
            ghcr.io/cscetbon/casskop-build:v1.13.0
          # push: true